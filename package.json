{
  "name": "json-to-sql",
  "version": "1.0.0",
  "description": "",
  "bin": {
    "@yardgnomeninja/json-to-sql": "bin/json-to-sql",
    "json-to-sql": "bin/json-to-sql"
  },
  "scripts": {
    "start": "tsc && node dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "msnodesqlv8": "^0.8.10",
    "mssql": "^6.1.0"
  }
}
